import os

def generate_tasks_with_ai(goal, intensity):
    """
    Generates task ideas using an AI model.
    """

    # Placeholder for now (replace with real API later)
    if intensity == "low":
        return [
            f"Light revision for {goal}",
            f"Review basics related to {goal}"
        ]
    else:
        return [
            f"Study core concepts for {goal}",
            f"Practice problems related to {goal}",
            f"Build a small feature aligned with {goal}"
        ]
